import {StyleSheet} from 'react-native';

const GlobalStyles = StyleSheet.create({
  root: {
    flex: 1,
    backgroundColor: '#fff',
  },
  errorMsg: {
    color: 'red',
    fontSize: 12,
  },
});
export default GlobalStyles;
